# Build outputs
.next
out
build
coverage

# Dependencies
node_modules

# Generated
.prisma
**/*.tsbuildinfo

# Lockfile — owned by pnpm, not a source file. Reformatting it produces a huge
# diff on every install and risks confusing the resolver.
pnpm-lock.yaml

# Drizzle Kit output — regenerated by `drizzle-kit generate`, so formatting it
# just churns on the next migration.
drizzle/meta

# Vendored skills — upstream owns their formatting.
.agents/skills
skills-lock.json

# Eval report viewer — mostly one HTML/CSS template string. Prettier formats it as TypeScript,
# which rewraps the markup and CSS into something harder to read than the source.
eval/viewer.mts

# Generated eval reports (gitignored, but keep them out of `format` runs too).
eval/results
